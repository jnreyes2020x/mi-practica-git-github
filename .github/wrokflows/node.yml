name: Test Actions

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Mensaje de prueba
        run: echo "Hola desde GitHub Actions"